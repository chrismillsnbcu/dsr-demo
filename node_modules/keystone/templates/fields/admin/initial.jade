.field(class='type-' + field.type)
	.field-ui(class='width-' + field.width)
		if field.noedit
			.field-value=field.path
		else
			input(type='hidden', name=field.path, value=field.path, autocomplete='off').form-control
		if field.note
			.field-note!= field.note
